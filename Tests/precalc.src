i32 sum(ConstSpan<i32> a)
{
	i32 s = 0;
	for (i32 i = 0; i < a.Length; i++)
	{
		s = s + a[i];
	}
	return s;
}

i32 main()
{
	i32 c = #run sum([1, 2, 3, 4]:i32);
	WriteLine("Precalc: " + to_str(c));

	i32 s = sum([1, 2, 3, 4]:i32);
	WriteLine("Sum: " + to_str(s));

	return 0;
}
