// An extern's `#output` is a reference the platform layer writes through, not a port onto a net.
extern i32 adc_read(i32 channel, #output i32 count @ "rx_count");

i32 main()
{
	i32 n = 0;
	WriteLine(to_str(adc_read(0, n)));
	return 0;
}
