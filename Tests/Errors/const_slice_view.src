// `span_slice` returns a view OF its argument, so slicing a const buffer must not hand back a mutable one.
i32 main()
{
	const i32[4] a = [1, 2, 3, 4]:i32;
	Span<i32> part = span_slice<i32>(a, 0:u32, 2:u32);
	part[0] = 9;
	WriteLine(to_str(a[0]));
	return 0;
}
