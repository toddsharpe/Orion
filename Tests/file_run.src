// `#run { }` at file scope, hoisted to the top of the entry: an included file's run goes first.
#using "Lib/file_run_lib.src"

#run
{
	#assert(2 + 2 == 4, "includer: arithmetic");
	#insert { WriteLine("includer file-run ran second"); }
}

i32 main()
{
	WriteLine("main body");
	return 0;
}
